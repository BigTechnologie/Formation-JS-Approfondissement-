"use stricte";

const books = new Books();

async function initBooks() {
    try {
        await books.load();

        // Construction de l'affichage
        bo
    } catch (error) {
        
    }
}